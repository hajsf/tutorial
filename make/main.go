package main

import "fmt"

func main() {
	fmt.Println("Hello")
}

// make build // to build binary for current system
//OR
// make all // to run the makefile command named `all` which in this case run all other commands in the same file
